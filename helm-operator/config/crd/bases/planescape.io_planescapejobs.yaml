apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: planescapejobs.planescape.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
spec:
  group: planescape.io
  names:
    kind: PlanescapeJob
    listKind: PlanescapeJobList
    plural: planescapejobs
    shortNames:
    - pjob
    - jobs
    singular: planescapejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Schedule of the job
      jsonPath: .spec.schedule
      name: Schedule
      type: string
    - description: Status of the job
      jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - description: Age of the job
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PlanescapeJob is the Schema for the planescapejobs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object.'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents.'
            type: string
          metadata:
            type: object
          spec:
            description: PlanescapeJobSpec defines the desired state of PlanescapeJob
            properties:
              jobTemplate:
                description: JobTemplate defines the job template
                properties:
                  spec:
                    description: JobSpec describes how the job execution will look like
                    properties:
                      template:
                        description: PodTemplateSpec describes the data a pod should have when created from a template
                        properties:
                          metadata:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          spec:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                        type: object
                    type: object
                type: object
              schedule:
                description: Schedule defines when the job should run (cron format or @once)
                type: string
              vault:
                description: Vault configuration for the job
                properties:
                  enabled:
                    type: boolean
                  path:
                    description: Vault path for secrets
                    type: string
                  role:
                    description: Vault role for authentication
                    type: string
                  ttl:
                    description: TTL for secrets
                    type: string
                type: object
            required:
            - jobTemplate
            - schedule
            type: object
          status:
            description: PlanescapeJobStatus defines the observed state of PlanescapeJob
            properties:
              conditions:
                description: Conditions represent the latest available observations of the job's state
                items:
                  description: Condition represents an observation of an object's state
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status to another
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about last transition
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's last transition
                      type: string
                    status:
                      description: Status of the condition
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              deployedRelease:
                description: Information about the deployed Helm release
                properties:
                  manifest:
                    type: string
                  name:
                    type: string
                  revision:
                    type: integer
                type: object
              lastScheduleTime:
                description: Information when was the last time the job was successfully scheduled
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {} 